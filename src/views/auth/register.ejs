<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="shortcut icon"
      href="../multimedia/branding/isotype.svg"
      type="image/x-icon"
    />
    <link rel="stylesheet" href="/css/styles.css" />
    <title>Register</title>
  </head>

  <body>
    <!-- HEADER -->
    <section class="admin">
      <h2 class="title1">CREA TU CUENTA</h2>
      <h3 class="subtitle">
        Completa el formulario para ser parte del mundo de los Funkos
      </h3>

      <form class="register-form" method="post" action="/auth/register">

      
        <% if (typeof errors != 'undefined') { %>
            <ul>
              <% errors.forEach(error => { %>
              <li><%= error.msg %></li>
              <% }) %>
            </ul>
            <% } %>


        <input type="text" name="name" placeholder="Nombre" id="name" value="<%= typeof values != 'undefined' ? values.name : '' %>">

        <input type="text" name="surname" placeholder="Apellido" id="surname" value="<%= typeof values != 'undefined' ? values.surname : '' %>">

        <input
          type="email"
          name="email"
          placeholder="Email"
          id="email"
          value="<%= typeof values != 'undefined' ? values.email : '' %>"
        />
        <input
            type="password"
            id="password"
            name="password"
            autocomplete="off"
            placeholder="Contraseña"
          />
          <input
          type="password"
          id="password_confirmation"
          name="password_confirmation"
          placeholder="Confirma tu contraseña"
        />
        <div class="botones">
          <button type="submit"  class="btn-ingresar">Registrarse</button>


          <label for="remember" class="remember"
            >Acepto terminos y condiciones<input
              type="checkbox"
              name="remember"
          /></label>
        </div>
      </form>
    </section>

  </body>
</html>
